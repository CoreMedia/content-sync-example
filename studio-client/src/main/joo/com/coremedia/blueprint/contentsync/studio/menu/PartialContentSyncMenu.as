package com.coremedia.blueprint.contentsync.studio.menu {
import ext.menu.Menu;

public class PartialContentSyncMenu extends Menu{
  public function PartialContentSyncMenu(config:PartialContentSyncMenu = null) {
    super(config);
  }

  override protected function afterRender():void {
    super.afterRender();

  }
}
}
